<Window
    x:Class="RandomPicker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RandomPicker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏（延伸进标题栏，Mica 材质） -->
        <Grid x:Name="AppTitleBar" Height="48" Background="Transparent">
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center" Spacing="10">
                <TextBlock Text="随机点名" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>

        <NavigationView x:Name="Nav" Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        OpenPaneLength="200"
                        SelectionChanged="Nav_SelectionChanged">

            <!-- 班级列表由代码动态填充 -->
            <NavigationView.MenuItems>
                <NavigationViewItemSeparator/>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="班级管理" Tag="manage" IsSelected="False">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE70F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="关于" Tag="about">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE946;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Grid>
                <Frame x:Name="RootFrame"/>
            </Grid>
        </NavigationView>

        <!-- 首次使用的 WinUI 3 引导气泡 -->
        <TeachingTip x:Name="WelcomeTip"
                     Title="欢迎使用随机点名"
                     CloseButtonText="开始使用"
                     IsLightDismissEnabled="False"
                     PreferredPlacement="BottomRight">
            <StackPanel Spacing="6">
                <TextBlock Text="· 左侧点击班级名称即可切换班级"/>
                <TextBlock Text="· 打开「班级管理」可导入 TXT 名单"/>
                <TextBlock Text="· 点名台按空格键即可开始 / 停止"/>
            </StackPanel>
        </TeachingTip>
    </Grid>
</Window>
